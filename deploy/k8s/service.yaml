apiVersion: v1
kind: Service
metadata:
  name: m3-slr-infer
  namespace: m3-slr
spec:
  type: NodePort
  selector:
    app: m3-slr-infer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30081
